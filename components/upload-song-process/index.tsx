'use client';

import { Button } from '../primitives/button';

export default function UploadSongProcess() {
  const onSubmit = async () => {
  };
  return <Button onClick={onSubmit}>Buscar canción</Button>;
}
